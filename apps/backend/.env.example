# Database connection URL
DATABASE_URL="postgresql://username:password@localhost:5432/synect?schema=public"

# API runtime
PORT=3001

# Auth
JWT_SECRET="replace-with-a-strong-secret"
JWT_EXPIRES_IN="1d"
