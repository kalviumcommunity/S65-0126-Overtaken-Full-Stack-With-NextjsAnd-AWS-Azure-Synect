# Database Configuration
POSTGRES_USER=synect_user
POSTGRES_PASSWORD=synect_password
POSTGRES_DB=synect_db
DB_PORT=5432

# Backend Configuration
BACKEND_PORT=3000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-change-in-production

# Frontend Configuration
FRONTEND_PORT=3001
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Database URL (auto-generated from above, but can be overridden)
# DATABASE_URL=postgresql://synect_user:synect_password@db:5432/synect_db
